# Library-Management-System
Java student project

Console application that allows user to:
1) create account
2) login
3) edit profile
4) borrow a book
5) return a book

Admin can:
1) edit password
2) edit (add, update, remove) books database
3) edit (add, update, remove) users database

Whole project includes numerous validations of user input.
